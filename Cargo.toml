[package]
name = "gitcore"
version = "0.1.0"
edition = "2018"
rust-version = "1.85.0"

[lib]
crate-type = ["staticlib"]

[dependencies]
bitfield-struct = "0.9"
clap = { version = "4.5", features = ["derive"] }
libc = "0.2"
